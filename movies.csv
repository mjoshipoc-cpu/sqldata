Create Table Provider_scorecard(
Request_instance_id numeric(11,0) NULL,
Book_NM varchar(10) NULL,
Member_ID varchar(50) NULL,
Member_Gender varchar(50) NULL,
Group_id varchar(50) NULL,
Provider_Name varchar(50) NULL,
Provider_ID varchar(50) NULL,
Tin_name varchar(50) NULL,
Provider_TIN varchar(50) NULL,
Provider_Address varchar(50) NULL,
Provider_city varchar(50) NULL,
Provider_state varchar(50) NULL,
Provider_zip varchar(50) NULL,
Provider_county varchar(50) NULL,
Provider_Phone varchar(50) NULL,
Provider_Specialty varchar(50) NULL,
BCS_GAP varchar(5) NULL,
COL_GAP varchar(5) NULL,
TRCMR_GAP varchar(5) NULL,
TRCPE_GAP varchar(5) NULL,
KED_GAP varchar(5) NULL,
CBP_GAP varchar(5) NULL,
GSD_GAP varchar(5) NULL,
FUH_GAP varchar(5) NULL,
PPC_GAP varchar(5) NULL,
SPC_GAP varchar(5) NULL,
SMC_GAP varchar(5) NULL,
Year int,
Quater int,
)


This is an urgent ask. I wqnt you to create a schema of the table used for GIC reporting in Hedis in the EXL domain. We discussed it has provider id and address as well. We are looking to create KPIs around gap reporting from payer perspective. Also, please generate synthetic data for this and the provider as well so that we can pass this to Genie and create the needed dashboard at state county level. We expect to have prior years data as well so that we can compare provider performance for different measures across years.



